on:
  workflow_dispatch:
jobs:
  my_job:
    name: TEST
    runs-on: ubuntu-latest
    
    steps:
      - name: pwd
        run: pwd

      - name: ls
        run: ls

      - name: touch 
        run: touch text2.txt

      - name: ls2
        run: ls
